fcb_apache::config:
  purge_configs: false,
#  default_vhost: false
fcb_apache::purge_configs: false
fcb_apache::packages:
  - mod_security
  - mod_security_crs
fcb_apache::vhosts_defaults:
  nonssl:
    port: '8080'
    docroot: '/var/www/vhost'
  ssl:
    port: '443'
    ssl: true
