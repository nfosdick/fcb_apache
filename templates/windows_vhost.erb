<% @vhost.each do |server, port_config| %>
<% port_config.each do |port, config| %>
<VirtualHost *:<%= port %>>
 DocumentRoot "<%= port_config['DocumentRoot'] %>"
 DocumentRoot "<%= config['DocumentRoot'] %>"
 ServerName <%= server %>
</VirtualHost>
<% end %>
<% end %>

